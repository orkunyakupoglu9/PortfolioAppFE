# The frontend runs completely with local dummy API routes today.
# TODO(API): Set these when the real Spring Boot API and market stream are ready.
BACKEND_URL=http://localhost:8080
NEXT_PUBLIC_WEBSOCKET_URL=
