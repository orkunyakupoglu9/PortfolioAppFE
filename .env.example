# Spring Boot target used server-side by Next.js Route Handlers.
BACKEND_URL=http://localhost:8080

# Native STOMP WebSocket endpoint exposed to the browser at build time.
NEXT_PUBLIC_WEBSOCKET_URL=ws://localhost:8080/ws
